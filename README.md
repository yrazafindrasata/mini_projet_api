# mini_projet_api

#### Init

```bash
docker-compose up -d
docker-compose exec --user=application web bash
composer install

```

#### routes
```
127.0.0.1/region -> insee
127.0.0.1/search -> adresse/meteo
```
